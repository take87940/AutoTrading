import React from 'react';
import ReactDOM from 'react-dom/client';
import TrackETHContract2 from './Price2';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TrackETHContract2 />
  </React.StrictMode>
);

